# Testing commands

- Run a single package: `go test ./handoff`
- Run a single test: `go test ./... -run TestName`
- Run all tests: `go test ./...`
- Format changed files: `gofmt -w <file1.go> <file2.go>`
